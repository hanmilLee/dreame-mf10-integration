# Screenshots

Place repository screenshots here. The README references `dashboard.png`:

- `dashboard.png` — the Dreame MF10 device page / controls in Home Assistant.

To capture: open the device page in HA, screenshot the **Controls** card, save it as
`docs/images/dashboard.png`.
